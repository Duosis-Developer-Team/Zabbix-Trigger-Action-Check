---
- name: Zabbix Trigger Action Monitor
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Python ile DNS testi
      command: python3 -c "import socket; print(socket.gethostbyname('watchman.bulutistan.com'))"
      register: dns_test
      ignore_errors: yes

    - name: DNS sonucu
      debug:
        msg: "{{ dns_test.stdout | default(dns_test.stderr) }}"

    - name: curl ile baglanti testi
      command: curl -sk -o /dev/null -w "%{http_code}" https://watchman.bulutistan.com/api_jsonrpc.php
      register: curl_test
      ignore_errors: yes

    - name: curl sonucu
      debug:
        msg: "HTTP status: {{ curl_test.stdout | default(curl_test.stderr) }}"

    - name: Env degiskenlerini kontrol et
      debug:
        msg:
          - "ZABBIX_URL = {{ lookup('env', 'ZABBIX_URL') }}"
          - "MAILTO     = {{ lookup('env', 'MAILTO') }}"
          - "SMTP_SERVER= {{ lookup('env', 'SMTP_SERVER') }}"

    - name: Zabbix Action Monitor scriptini calistir
      command: python3 "{{ playbook_dir }}/zabbix_action_monitor.py"
      environment:
        ZABBIX_URL: "{{ lookup('env', 'ZABBIX_URL') }}"
        ZABBIX_API_TOKEN: "{{ lookup('env', 'ZABBIX_API_TOKEN') }}"
        MAILTO: "{{ lookup('env', 'MAILTO') }}"
        MAIL_FROM: "{{ lookup('env', 'MAIL_FROM') }}"
        SMTP_SERVER: "{{ lookup('env', 'SMTP_SERVER') }}"
        SMTP_PORT: "{{ lookup('env', 'SMTP_PORT') }}"
        SMTP_TLS: "{{ lookup('env', 'SMTP_TLS') | default('false') }}"
        EXCLUDE_ACTIONS: "{{ lookup('env', 'EXCLUDE_ACTIONS') }}"
      register: result
      # exit 0 = sorun yok, exit 2 = action disable edildi (changed)
      failed_when: result.rc not in [0, 2]
      changed_when: result.rc == 2

    - name: Sonucu logla
      debug:
        msg: "{{ result.stdout_lines }}"
      when: result.stdout_lines is defined
